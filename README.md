# `@simplifield/prettier-config`

> SimpliField shared [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @simplifield/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@simplifield/prettier-config"
}
```
